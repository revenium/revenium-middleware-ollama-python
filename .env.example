# Revenium Configuration (Required)
REVENIUM_METERING_API_KEY=hak_your_key_here
REVENIUM_METERING_BASE_URL=https://api.revenium.ai

# Ollama is local - no API key needed
# Ensure Ollama is running: ollama serve

# Logging (Optional)
REVENIUM_LOG_LEVEL=INFO

# ============================================================================
# Trace Visualization Fields (Optional)
# ============================================================================
# These fields enhance trace visualization and analytics in the Revenium dashboard.
# All fields are optional - if not set, they will be omitted from the metering data.
# The middleware will also check for fallback environment variables (see documentation).

# Environment name (e.g., 'production', 'staging', 'development')
# Fallbacks: ENVIRONMENT, DEPLOYMENT_ENV
# REVENIUM_ENVIRONMENT=production

# Cloud region (e.g., 'us-east-1', 'eastus', 'us-central1')
# Fallbacks: AWS_REGION, AWS_DEFAULT_REGION, AZURE_REGION, GCP_REGION, GOOGLE_CLOUD_REGION
# REVENIUM_REGION=us-east-1

# Human-readable credential identifier (e.g., 'prod-ollama-key', 'team-api-key')
# Helps identify which API key was used for the request
# REVENIUM_CREDENTIAL_ALIAS=prod-ollama-key

# Trace type - categorical identifier for grouping workflows
# Format: alphanumeric, hyphens, underscores only; max 128 chars
# Examples: 'customer-support', 'data-analysis', 'content-generation'
# REVENIUM_TRACE_TYPE=customer-support

# Trace name - human-readable label for trace instances
# Max 256 chars (auto-truncates with warning if longer)
# Examples: 'Customer Support Chat', 'Document Analysis', 'Email Generation'
# REVENIUM_TRACE_NAME=Customer Support Chat

# Parent transaction ID for distributed tracing
# Use this to link related operations across services
# REVENIUM_PARENT_TRANSACTION_ID=

# Transaction name - human-friendly operation label
# Examples: 'Generate Response', 'Analyze Sentiment', 'Summarize Document'
# REVENIUM_TRANSACTION_NAME=Generate Response

# Note: operationType and operationSubtype are auto-detected by the middleware
# based on the API endpoint and request parameters. You don't need to set them.
